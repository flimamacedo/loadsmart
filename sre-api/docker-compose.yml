services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION:-us-east-1}
      API_BASIC_USER: ${API_BASIC_USER:-admin}
      API_BASIC_PASSWORD: ${API_BASIC_PASSWORD:-changeme}
